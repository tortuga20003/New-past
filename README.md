# Implementação 

*Exemplo

'''
Author:
  Flávio__:D
'''

```python
def fatorial(n: int) -> int:
  if n < 0:
      raise ValueError("Número negativos não tem fatorial.")
  res = 1
  for 1 in range(1, n + 1):
return res